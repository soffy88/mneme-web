import { MnemeShell } from '@/components/layout/MnemeShell';

export default function ErrorJournalLayout({ children }: { children: React.ReactNode }) {
  return <MnemeShell>{children}</MnemeShell>;
}
